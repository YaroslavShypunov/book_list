import React from 'react';

interface IAddBookForm {
  name?: string;
}

const AddBookForm = ({ name }: IAddBookForm) => {
  return <div>{/* Your code here */}</div>;
};

export default React.memo(AddBookForm);
